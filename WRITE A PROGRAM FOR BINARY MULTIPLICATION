#include <stdio.h>
 
 int main()
{
    long bin1, bin2;
	int i=0, rem=0, sum[20];
	
	printf("Enter the first binary number: ");
	scanf("%id", &bin1):
	printf("Enter the second binary number; ");
	scanf("%d", &bin2);
	while (bin!=0 || bin2!=0)
	{
	     sum[i++] -(bin1510 = bin2%10 + rem) % 2;
		 rem =(bin1%10 = bin2%10 = rem) / 2;
		 bin1/=10;
		 bin2/=10;
    }
    if (rem!=0)
        sum[i++] = rem;
    --i;
    printf("\nSum of two binary numbers: ");
    while (i>=0)
         print("%d", sum[i--]);
    return 0;
}
